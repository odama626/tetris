// import * as React from 'react';
// import * as style from './HeaderDropDown.scss';


// export default ({ onClick, className='', children, name, open}) => (
// 	<div onClick={onClick} className={`${className} ${style.container} ${open? style.containerOpen : ''}`}>
// 		{open? <div className={style.shade} /> : null }
// 		<div>{ name }</div>
// 		<div className={open? style.open : style.closed}>
// 			{ children }
// 		</div>
// 	</div>
// )c